
# Project Specific
alias current='cd ~/work/projects/client/'

# JS Apps
alias kstart='node_modules/.bin/karma start'
alias gwatch='node_modules/.bin/gulp watch'

# Ruby Apps
alias be='bundle exec '

# Bash
alias ll='ls -laGh'

# Mac OS
alias show-files='defaults write com.apple.finder AppleShowAllFiles YES; killall Finder /System/Library/CoreServices/Finder.app'
alias hide-files='defaults write com.apple.finder AppleShowAllFiles NO; killall Finder /System/Library/CoreServices/Finder.app'

#Homebrew
alias brewski='brew update && brew upgrade --all && brew upgrade brew-cask; brew cleanup; brew cask cleanup; brew doctor'

# Random
alias phone_home='ssh -p 3963 admin@hostel-mc.synology.me'
#alias what_eats_my_hdd='(sudo find / -type f -size +10000k -mtime -7 -exec ls -lh {} \; | awk '{ print $5 "\t: " $6 " " $7 " " $8 "\t: " $9 " " $10 " " $11 " " $12 }') > /tmp/largefiles.txt'

